<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema principal -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Animaciones -->
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item> <!-- Ahora existe -->

        <!-- Estilo para botones -->
        <item name="buttonStyle">@style/MyButtonStyle</item>
    </style>

    <!-- Estilo de botón -->
    <style name="MyButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_margin">8dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Estilo de texto (se aplicará manualmente) -->
    <style name="MyTextStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
    <style name="MenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:gravity">center</item>
    </style>
    <style name="MenuItemButtonStyle">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
    <style name="MenuItemCenteredTextStyle">
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingStart">50dp</item> <!-- Ajusta este valor -->
        <item name="android:paddingEnd">50dp</item>   <!-- Ajusta este valor -->
    </style>
</resources>